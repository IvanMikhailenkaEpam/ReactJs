const initialState = {
  films: [],
  film: {},
  searchType: 'title',
  sortBy: 'age'
};

export function filmReducer(state = initialState, action) {
  switch (action.type) {

    default:
      return state
  }
}
